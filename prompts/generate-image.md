---
description: Generate an image with ImageGen
---
Use the `generate_image` tool for this request: $@

If no prompt was supplied, ask the user what image they want. Otherwise, generate the image directly and report where it was saved.
